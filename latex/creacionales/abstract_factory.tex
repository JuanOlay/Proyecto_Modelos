% ═══════════════════════════════════════════════════════════════════════════════
%  CAPÍTULO — Abstract Factory
% ═══════════════════════════════════════════════════════════════════════════════

\chapter{Abstract Factory}

% ─── Situación Problema ──────────────────────────────────────────────────────
\section{Situación Problema}

Airbnb opera en tres superficies distintas: aplicación web, aplicación
iOS y aplicación Android. Cada superficie tiene sus propios estándares
visuales: los botones, tarjetas de propiedad y formularios de reserva
difieren en apariencia y comportamiento entre plataformas, pero siempre
deben mantenerse visualmente coherentes \emph{dentro} de la misma
plataforma.

El equipo de frontend se encontró con que el código de composición de
pantallas mezclaba condicionales de plataforma con la lógica de negocio.
Crear un componente de tarjeta de propiedad para web y luego reutilizar
ese mismo constructor en la app de iOS producía inconsistencias visuales.
El problema se agravaba cada vez que se quería incorporar una nueva
plataforma: había que rastrear todos los puntos del código donde se
instanciaban componentes y añadir una nueva rama condicional.


% ─── Solución ────────────────────────────────────────────────────────────────
\section{Solución}

El patrón Abstract Factory define una interfaz \texttt{FabricaUI} con
métodos para crear cada familia de componentes (\texttt{crearBoton()},
\texttt{crearTarjetaPropiedad()}, \texttt{crearFormularioReserva()}).
Las fábricas concretas \texttt{FabricaWeb}, \texttt{FabricaIOS} y
\texttt{FabricaAndroid} implementan esa interfaz produciendo componentes
coherentes con su plataforma.

El código de composición de pantallas trabaja únicamente contra
\texttt{FabricaUI}; en tiempo de ejecución se inyecta la fábrica
correspondiente a la plataforma activa. Esto garantiza que los componentes
de una misma familia siempre sean compatibles entre sí, elimina los
condicionales de plataforma dispersos por el código y permite incorporar
nuevas superficies simplemente añadiendo una nueva fábrica concreta.

% ─── Diagrama de clases ─────────────────────────────────────────────────────
\begin{figure}[H]
    \centering
    % \includegraphics[width=\textwidth]{imagenes/abstract_factory_diagrama.png}
    \caption{Diagrama de clases del patrón Abstract Factory}
\end{figure}


% ─── Roles de las Clases ────────────────────────────────────────────────────
\section{Roles de las Clases}

% Explicar la responsabilidad de cada clase/interfaz

\begin{itemize}
    \item \textbf{AbstractFactory:}  
    La interfaz \texttt{FabricaUI} define los métodos necesarios para crear
    cada tipo de componente de la interfaz gráfica. Su objetivo es garantizar
    que el cliente pueda trabajar con familias completas de productos sin
    depender de implementaciones concretas.

    \item \textbf{ConcreteFactory:}  
    Las clases \texttt{FabricaWeb}, \texttt{FabricaIOS} y
    \texttt{FabricaAndroid} implementan la interfaz \texttt{FabricaUI} y se
    encargan de crear productos compatibles entre sí según la plataforma
    correspondiente. Cada fábrica representa una familia completa de
    componentes visuales.

    \item \textbf{AbstractProduct:}  
    Las interfaces \texttt{Boton}, \texttt{TarjetaPropiedad} y
    \texttt{FormularioReserva} definen el comportamiento común que deben
    cumplir todos los productos de una misma categoría, independientemente
    de la plataforma donde se utilicen.

\begin{itemize}
    \item \textbf{AbstractFactory:}  
    La interfaz \texttt{FabricaUI} define los métodos necesarios para crear
    las diferentes familias de componentes gráficos sin depender de clases
    concretas.

    \item \textbf{ConcreteFactory:}  
    Las clases \texttt{FabricaWeb}, \texttt{FabricaIOS} y
    \texttt{FabricaAndroid} implementan la fábrica abstracta y crean
    componentes compatibles con cada plataforma específica.

    \item \textbf{AbstractProduct:}  
    Las interfaces \texttt{Boton}, \texttt{TarjetaPropiedad} y
    \texttt{FormularioReserva} definen el comportamiento común de los
    productos de la interfaz.

    \item \textbf{ConcreteProduct:}  
    Las clases \texttt{BotonWeb}, \texttt{TarjetaWeb},
    \texttt{FormularioWeb}, \texttt{BotonIOS}, \texttt{TarjetaIOS},
    \texttt{FormularioIOS}, \texttt{BotonAndroid},
    \texttt{TarjetaAndroid} y \texttt{FormularioAndroid} representan los
    productos concretos de cada plataforma.

    \item \textbf{Client:}  
    La clase \texttt{Cliente} utiliza únicamente la interfaz
    \texttt{FabricaUI} para obtener los componentes necesarios. El cliente no
    conoce las clases concretas que instancia, lo que permite cambiar de
    plataforma sin modificar la lógica principal de la aplicación.
\end{itemize}


% ─── Main ───────────────────────────────────────────────────────────────────
\section{Main}

\begin{codigof}{AbtractFactory.java}
class AbstractFactory {
    public static void main(String[] args) {
        Cliente clienteWEB = new Cliente(new FabricaWeb());
        Cliente clienteIOS = new Cliente(new FabricaIOS());
        Cliente clienteAndroid = new Cliente(new FabricaAndroid());

        Cliente[] clientes = {clienteWEB, clienteIOS, clienteAndroid};

        for (Cliente cliente : clientes) {
            System.out.println(cliente);
            System.out.println(cliente.obtenerVista());
            System.out.println();
        }
    }
}

\end{codigof}


% ─── Salida del Main ────────────────────────────────────────────────────────
\section{Salida del Main}

\begin{salida}
WEB
Tarjeta Web
Formulario Web
Boton Web

IOS
Tarjeta iOS
Formulario iOS
Boton iOS

ANDROID
Tarjeta Android
Formulario Android
Boton Android
\end{salida}


% ─── Clases ─────────────────────────────────────────────────────────────────
\section{Clases}

\begin{codigof}{Abstract Products}
interface Boton {
    String render();
}

interface TarjetaPropiedad {
    String render();
}

interface FormularioReserva {
    String render();
}
\end{codigof}

\begin{codigof}{Abstract Factory}
 interface FabricaUI {
    Boton crearBoton();
    TarjetaPropiedad crearTarjetaPropiedad();
    FormularioReserva crearFormularioReserva();
}
\end{codigof}

\begin{codigof}{Concrete Products}
// Familia Web
class BotonWeb implements Boton {
    public String render() { return "Boton Web"; }
}
class TarjetaWeb implements TarjetaPropiedad {
    public String render() { return "Tarjeta Web"; }
}
class FormularioWeb implements FormularioReserva {
    public String render() { return "Formulario Web"; }
}

// Familia iOS
class BotonIOS implements Boton {
    public String render() { return "Boton iOS"; }
}
class TarjetaIOS implements TarjetaPropiedad {
    public String render() { return "Tarjeta iOS"; }
}
class FormularioIOS implements FormularioReserva {
    public String render() { return "Formulario iOS"; }
}

// Familia Android
class BotonAndroid implements Boton {
    public String render() { return "Boton Android"; }
}
class TarjetaAndroid implements TarjetaPropiedad {
    public String render() { return "Tarjeta Android"; }
}
class FormularioAndroid implements FormularioReserva {
    public String render() { return "Formulario Android"; }
}
\end{codigof}

\begin{codigof}{Concrete Factories}
class FabricaWeb implements FabricaUI {
    public Boton crearBoton() { return new BotonWeb(); }
    public TarjetaPropiedad crearTarjetaPropiedad() { return new TarjetaWeb(); }
    public FormularioReserva crearFormularioReserva() { return new FormularioWeb(); }
}

class FabricaIOS implements FabricaUI {
    public Boton crearBoton() { return new BotonIOS(); }
    public TarjetaPropiedad crearTarjetaPropiedad() { return new TarjetaIOS(); }
    public FormularioReserva crearFormularioReserva() { return new FormularioIOS(); }
}

class FabricaAndroid implements FabricaUI {
    public Boton crearBoton() { return new BotonAndroid(); }
    public TarjetaPropiedad crearTarjetaPropiedad() { return new TarjetaAndroid(); }
    public FormularioReserva crearFormularioReserva() { return new FormularioAndroid(); }
}
\end{codigof}

\begin{codigof}{Client}
class Cliente {
    private final Boton boton;
    private final TarjetaPropiedad tarjeta;
    private final FormularioReserva formulario;

    private final FabricaUI fabrica;

    Cliente(FabricaUI fabrica) {
        this.boton = fabrica.crearBoton();
        this.tarjeta = fabrica.crearTarjetaPropiedad();
        this.formulario = fabrica.crearFormularioReserva();
        this.fabrica = fabrica;
    }

    String obtenerVista() {
        return tarjeta.render() + "\n" + formulario.render() + "\n" + boton.render();
    }

    @Override
    public String toString() {
        if (fabrica instanceof FabricaWeb) {
            return "WEB";
        } else if (fabrica instanceof FabricaIOS) {
            return "IOS";
        } else if (fabrica instanceof FabricaAndroid) {
            return "ANDROID";
        }

        return "DESCONOCIDO";
    }
}
\end{codigof}

% ─── Conclusiones ───────────────────────────────────────────────────────────
\section{Conclusiones}

El patrón \textit{Abstract Factory} permite crear familias de objetos
relacionados manteniendo coherencia entre sus componentes. En este caso,
facilita la creación de interfaces específicas para Web, iOS y Android sin
mezclar la lógica de negocio con detalles de implementación visual.

Además, reduce el acoplamiento entre el cliente y las clases concretas,
permitiendo agregar nuevas plataformas de manera más sencilla. Sin
embargo, puede incrementar la cantidad de clases del sistema y hacer más
compleja la estructura inicial del proyecto.