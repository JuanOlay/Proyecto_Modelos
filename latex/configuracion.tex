% ─────────────────────────────────────────────────────────────────────────────
%  configuracion.tex  —  Optimizado
% ─────────────────────────────────────────────────────────────────────────────

% --- Interlineado ------------------------------------------------------------
\usepackage{setspace}
\onehalfspacing

% --- Sangría -----------------------------------------------------------------
\setlength{\parindent}{0.5cm}
\setlength{\parskip}{6pt}

\setlength{\headheight}{14.5pt}
\addtolength{\topmargin}{-2.5pt}

% --- Encabezados -------------------------------------------------------------
\pagestyle{fancy}
\fancyhf{}

\fancyhead[L]{\small\textit{Patrones de Diseño — Airbnb}}
\fancyhead[R]{\small\leftmark}

\fancyfoot[C]{\thepage}

\renewcommand{\headrulewidth}{0.4pt}

% --- Estilo de código --------------------------------------------------------
\lstdefinestyle{estilo-salida}{
  basicstyle       = \ttfamily\small,
  backgroundcolor  = \color{gray!8},
  frame            = single,
  framerule        = 0.5pt,
  rulecolor        = \color{gray!40},
  breaklines       = true,
  showstringspaces = false,
  numbers          = none,
  keywordstyle     = {},
  commentstyle     = {},
  stringstyle      = {},
  identifierstyle  = {}
}

\lstnewenvironment{salida}{%
  \lstset{style=estilo-salida}%
}{}

% --- Estilo de código con minted (optimizado para Java) ----------------------

\newcommand{\code}[1]{%
  \mintinline[bgcolor=gray!10, fontsize=\small]{java}{#1}%
}

% --- Entorno flotante con caja  →  \begin{codigof}{Título} ------------------
\newtcblisting{codigof}[1]{
  listing only,           % ← muestra solo el código, sin panel inferior
  listing engine  = minted,
  minted language = java,
  minted options  = {
    style      = vs,
    linenos    = true,
    numbersep  = 6pt,
    breaklines = true,
    tabsize    = 4,
    fontsize   = \small
  },
  title      = {#1},
  enhanced,
  breakable,
  colback    = gray!4,
  colframe   = NavyBlue!60,
  arc        = 4pt,
  leftrule   = 3pt,
  rightrule  = 0.5pt,
  toprule    = 0.5pt,
  bottomrule = 0.5pt,
  fontupper  = \small
}

% --- Entorno simple sin flotante  →  \begin{codigos} ------------------------
\newtcblisting{codigos}{
  listing only,           % ← ídem
  listing engine  = minted,
  minted language = java,
  minted options  = {
    style      = vs,
    linenos    = true,
    numbersep  = 6pt,
    breaklines = true,
    tabsize    = 4,
    fontsize   = \small
  },
  enhanced,
  breakable,
  colback    = gray!4,
  colframe   = NavyBlue!40,
  arc        = 4pt,
  leftrule   = 3pt,
  rightrule  = 0.5pt,
  toprule    = 0.5pt,
  bottomrule = 0.5pt,
  fontupper  = \small
}

% --- Títulos -----------------------------------------------------------------
\usepackage{titlesec}

\titleformat{\chapter}[display]
  {\normalfont\Large\bfseries\color{NavyBlue}}
  {\chaptertitlename\ \thechapter}
  {10pt}
  {\Huge}

\titleformat{\section}
  {\normalfont\large\bfseries\color{NavyBlue!80}}
  {\thesection}
  {1em}
  {}

\titleformat{\subsection}
  {\normalfont\normalsize\bfseries\color{NavyBlue!60}}
  {\thesubsection}
  {1em}
  {}

% --- Captions ----------------------------------------------------------------
\captionsetup{
  font      = small,
  labelfont = {bf,color=NavyBlue},
  labelsep  = period
}

% --- Numeración continua de figuras ------------------------------------------
% Descomentar solo si lo necesitas
%
% \counterwithout{figure}{chapter}

% --- Macro reutilizable ------------------------------------------------------
\newcommand{\patroninfo}[2]{%
  \vspace{4pt}
  \noindent
  \begin{tabular}{@{}ll}
    \textbf{Tipo:}       & #1 \\
    \textbf{GoF (pág.):} & #2 \\
  \end{tabular}
  \vspace{6pt}
}